{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run Causal Canvas",
      "type": "extensionHost",
      "request": "launch",
      "args": ["--extensionDevelopmentPath=${workspaceFolder}/apps/vscode", "${workspaceFolder}"],
      "outFiles": ["${workspaceFolder}/apps/vscode/dist/**/*.cjs"]
    }
  ]
}
